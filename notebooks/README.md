# Notebooks

| Notebook | Purpose |
|----------|---------|
| `01_eda_preprocessing.ipynb` | Task 1 — exploratory data analysis and text cleaning |
| `02_chunking_embedding.ipynb` | Task 2 — sampling, chunking, embedding, and vector store indexing (exploration; final pipeline lives in `src/`) |

Keep exploration here; move reusable, tested logic into `src/` modules.
